eval ulimit -c unlimited

su -c "python /usr/share/xbmc/FEH.py --standalone --lirc /dev/lircd" vdr
RET=$?
if [ $RET -ne 0 ]; then
  exit $RET
fi

exec su -c "/usr/lib/xbmc/xbmc.bin --standalone --lirc /dev/lircd" vdr
end script
