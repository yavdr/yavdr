<%pre>
#include <ClearSilver.h>
#include "../common.h"
</%pre>

<%args>
name;
value;
</%args>

<%cpp>
  HDF *hdfdb;
  hdf_init(&hdfdb);
  hdf_read_file(hdfdb, YAVDRDB);
  hdf_set_value(hdfdb, name.c_str(), value.c_str());
  hdf_write_file(hdfdb, YAVDRDB);
  hdf_destroy(&hdfdb);
</%cpp>
