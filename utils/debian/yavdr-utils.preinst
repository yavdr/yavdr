#!/bin/sh

if [ install = "$1" -o upgrade = "$1" ]; then 
   dpkg-divert --package yavdr-utils --add --rename \
      --divert /etc/firefox-3.5/profile/prefs.js.dontremove /etc/firefox-3.5/profile/prefs.js
fi  

