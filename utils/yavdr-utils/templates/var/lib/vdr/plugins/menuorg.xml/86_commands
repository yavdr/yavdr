         <menu name="Befehle">
               <command name="USB Massenspeicher sicher entfernen" execute="HOME=/var/lib/vdr /usr/bin/halevt-umount -ra &amp;> /dev/null" />
               <command name="Aufnahmen-Liste aktualisieren" execute="echo '/usr/bin/touch /srv/vdr/video.00/.update' | at now" />
	       <command name="VDR neu starten?" execute="stop vdr ; start vdr" />
               <command name="Rechner Neu starten" execute="/usr/bin/signal-event reboot" />
               <command name="System herunterfahren" execute="echo '/usr/bin/svdrpsend hitk power' | at now" />
         </menu>
