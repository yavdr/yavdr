#
# auto.net.yavdr
#
# Automounter map controlled by yavdrweb. Do not make any changes
# here! They will be overwriten by the webserver

<?cs each:entry = system.net.media.video ?>
  <?cs if:entry != "" ?>
video.<?cs name:entry ?> -fstype=nfs,hard,intr,nodev,nosuid,nonstrict,async <?cs var:entry ?>
  <?cs /if ?>
<?cs /each ?>
