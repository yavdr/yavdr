<?cs if:system.deinterlacer.type == "sd_half_temporal" ?>
# vdpau: SD deinterlace method
# { bob  half temporal  half temporal_spatial  temporal  temporal_spatial }, default: 3
video.output.vdpau_sd_deinterlace_method:half temporal

<?cs /if ?>

